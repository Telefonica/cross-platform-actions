{
  "name": "write-secret",
  "private": true,
  "description": "Github action to sync a deploy manifest with a project repositories",
  "scripts": {
    "test": "jest --config jest.config.js",
    "check:types:test": "tsc --noEmit --project ./test/unit/tsconfig.json",
    "check:types:lib": "tsc --noEmit",
    "check:types": "npm run check:types:test && npm run check:types:lib",
    "build": "ncc build src/index.ts -o dist --source-map",
    "prepare": "cd ../ && husky install"
  },
  "keywords": [],
  "author": "Telefónica",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "1.10.0",
    "@octokit/core": "4.2.0",
    "@octokit/plugin-rest-endpoint-methods": "7.2.3",
    "libsodium-wrappers": "0.7.11",
    "octokit": "2.0.14",
    "zod": "3.21.4"
  },
  "devDependencies": {
    "@jest/globals": "29.5.0",
    "@octokit/types": "9.3.1",
    "@types/jest": "29.5.2",
    "@types/libsodium-wrappers": "0.7.10"
  }
}